<div>
    <div class="mb-6 flex items-center justify-between"><div><h1 class="text-2xl font-bold text-slate-900">Historial de caja</h1><p class="text-sm text-slate-500">Sesiones y arqueos realizados.</p></div><a href="{{ route('cash.index') }}" class="rounded-lg bg-indigo-600 px-4 py-2 text-sm font-semibold text-white">Caja actual</a></div>
    <div class="rounded-xl bg-white p-5 shadow-sm"><div class="flex flex-wrap gap-3"><select wire:model.live="status" class="rounded-lg border-slate-300 text-sm"><option value="">Estados</option>@foreach($statuses as $item)<option value="{{ $item->value }}">{{ $item->value === 'open' ? 'Abierta' : 'Cerrada' }}</option>@endforeach</select><input type="date" wire:model.live="from" class="rounded-lg border-slate-300 text-sm"><input type="date" wire:model.live="to" class="rounded-lg border-slate-300 text-sm"></div><div class="mt-5 overflow-x-auto"><table class="w-full text-left text-sm"><thead class="border-b text-xs uppercase text-slate-500"><tr><th class="px-3 py-3">Caja / apertura</th><th class="px-3 py-3">Abrió</th><th class="px-3 py-3">Cierre</th><th class="px-3 py-3">Diferencia</th><th class="px-3 py-3">Estado</th><th></th></tr></thead><tbody class="divide-y divide-slate-100">@forelse($sessions as $session)<tr><td class="px-3 py-3">{{ $session->register?->name }}<div class="text-xs text-slate-500">{{ $session->opened_at?->format('d/m/Y H:i') }}</div></td><td class="px-3 py-3">{{ $session->opener?->name }}</td><td class="px-3 py-3">{{ $session->closed_at?->format('d/m/Y H:i') ?? '—' }}</td><td class="px-3 py-3">{{ $session->difference !== null ? number_format((float) $session->difference, 2) : '—' }}</td><td class="px-3 py-3">{{ $session->status->value === 'open' ? 'Abierta' : 'Cerrada' }}</td><td class="px-3 py-3 text-right"><a href="{{ route('cash.sessions.show', $session) }}" class="text-indigo-600">Ver libro diario</a></td></tr>@empty<tr><td colspan="6" class="px-3 py-10 text-center text-slate-500">No hay sesiones.</td></tr>@endforelse</tbody></table></div><div class="mt-4">{{ $sessions->links() }}</div></div>
</div>

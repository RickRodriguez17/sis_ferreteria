<div>
    <div class="mb-6 flex items-start justify-between"><div><a href="{{ route('credits.index') }}" class="text-sm text-indigo-600">← Créditos</a><h1 class="mt-2 text-2xl font-bold text-slate-900">Estado de cuenta</h1><p class="text-sm text-slate-500">{{ $credit->customer?->name }}</p></div><div class="flex gap-2">@can('create', \App\Models\CreditPayment::class) @if((float) $credit->balance > 0 && $credit->status->value !== 'cancelled')<button wire:click="openPayment" class="rounded-lg bg-indigo-600 px-4 py-2 text-sm font-semibold text-white">Registrar cobro</button>@endif @endcan</div></div>
    @if(session('success'))<div class="mb-4 rounded-lg bg-emerald-50 p-3 text-sm text-emerald-700">{{ session('success') }}</div>@endif
    <div class="grid gap-4 md:grid-cols-4"><div class="rounded-xl bg-white p-4 shadow-sm"><div class="text-xs uppercase text-slate-500">Original</div><div class="mt-1 text-xl font-bold">{{ number_format((float) $credit->original_amount, 2) }}</div></div><div class="rounded-xl bg-white p-4 shadow-sm"><div class="text-xs uppercase text-slate-500">Pagado</div><div class="mt-1 text-xl font-bold text-emerald-700">{{ number_format((float) $credit->paid_amount, 2) }}</div></div><div class="rounded-xl bg-white p-4 shadow-sm"><div class="text-xs uppercase text-slate-500">Saldo pendiente</div><div class="mt-1 text-xl font-bold text-rose-700">{{ number_format((float) $credit->balance, 2) }}</div></div><div class="rounded-xl bg-white p-4 shadow-sm"><div class="text-xs uppercase text-slate-500">Vencimiento</div><div class="mt-1 text-xl font-bold">{{ $credit->due_date?->format('d/m/Y') ?? 'Sin fecha' }}</div></div></div>
    <div class="mt-6 rounded-xl bg-white p-5 shadow-sm"><h2 class="font-semibold text-slate-900">Venta de origen</h2><div class="mt-3 text-sm text-slate-600">Código: {{ $credit->sale?->code ?? 'Sin venta' }} · Total: {{ number_format((float) ($credit->sale?->total ?? $credit->original_amount), 2) }}</div></div>
    <div class="mt-6 rounded-xl bg-white p-5 shadow-sm"><h2 class="font-semibold text-slate-900">Historial de cobros</h2><div class="mt-4 overflow-x-auto"><table class="w-full text-left text-sm"><thead class="border-b text-xs uppercase text-slate-500"><tr><th class="px-3 py-3">Fecha</th><th class="px-3 py-3">Usuario</th><th class="px-3 py-3">Caja</th><th class="px-3 py-3">Forma de pago</th><th class="px-3 py-3">Monto</th><th class="px-3 py-3">Observaciones</th></tr></thead><tbody class="divide-y divide-slate-100">@forelse($credit->payments as $payment)<tr><td class="px-3 py-3">{{ $payment->paid_at?->format('d/m/Y H:i') }}</td><td class="px-3 py-3">{{ $payment->creator?->name ?? 'Sistema' }}</td><td class="px-3 py-3">{{ $payment->cashSession?->id ?? 'Sin caja' }}</td><td class="px-3 py-3">{{ ['cash' => 'Efectivo', 'qr' => 'QR', 'transfer' => 'Transferencia'][$payment->method->value] ?? $payment->method->value }}</td><td class="px-3 py-3 font-semibold">{{ number_format((float) $payment->amount, 2) }}</td><td class="px-3 py-3">{{ $payment->notes ?: '—' }}</td></tr>@empty<tr><td colspan="6" class="px-3 py-10 text-center text-slate-500">Aún no hay cobros registrados.</td></tr>@endforelse</tbody></table></div></div>
    <livewire:payment-form :credit="$credit" />
</div>

<div><div class="mb-6"><h1 class="text-2xl font-bold text-slate-900">Existencias</h1><p class="text-sm text-slate-500">Stock actual por producto y ubicación.</p></div><div class="rounded-xl bg-white p-5 shadow-sm"><div class="flex flex-wrap gap-3"><select wire:model.live="locationId" class="rounded-lg border-slate-300 text-sm"><option value="">Todas las ubicaciones</option>@foreach($locations as $location)<option value="{{ $location->id }}">{{ $location->name }}</option>@endforeach</select><label class="flex items-center gap-2 text-sm"><input type="checkbox" wire:model.live="belowMinimum" class="rounded border-slate-300 text-indigo-600"> Solo bajo mínimo</label></div><div class="mt-5 overflow-x-auto"><table class="w-full text-left text-sm"><thead class="border-b text-xs uppercase text-slate-500"><tr><th class="px-3 py-3">Producto</th><th class="px-3 py-3">Ubicación</th><th class="px-3 py-3">Cantidad</th><th class="px-3 py-3">Mínimo</th><th class="px-3 py-3">Estado</th></tr></thead><tbody class="divide-y divide-slate-100">@forelse($items as $item)<tr><td class="px-3 py-3 font-medium">{{ $item->product->name }}</td><td class="px-3 py-3">{{ $item->location->name }}</td><td class="px-3 py-3 {{ $item->product->min_stock !== null && (float) $item->quantity < (float) $item->product->min_stock ? 'font-bold text-rose-600' : '' }}">{{ $item->quantity }}</td><td class="px-3 py-3">{{ $item->product->min_stock ?? '—' }}</td><td class="px-3 py-3">{{ $item->product->min_stock !== null && (float) $item->quantity < (float) $item->product->min_stock ? 'Bajo mínimo' : 'Normal' }}</td></tr>@empty<tr><td colspan="5" class="px-3 py-12 text-center text-slate-500">No hay existencias.</td></tr>@endforelse</tbody></table></div></div></div>

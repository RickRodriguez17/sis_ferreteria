<div>
    <div class="mb-6 flex items-center justify-between"><div><h1 class="text-2xl font-bold text-slate-900">Cuentas de cobro</h1><p class="text-sm text-slate-500">Cuentas disponibles para QR y transferencias.</p></div>@can('create', \App\Models\PaymentAccount::class)<button wire:click="create" class="rounded-lg bg-indigo-600 px-4 py-2 text-sm font-semibold text-white">Nueva cuenta</button>@endcan</div>
    @if(session('success'))<div class="mb-4 rounded-lg bg-emerald-50 p-3 text-sm text-emerald-700">{{ session('success') }}</div>@endif
    <div class="rounded-xl bg-white p-5 shadow-sm"><table class="w-full text-left text-sm"><thead class="border-b text-xs uppercase text-slate-500"><tr><th class="px-3 py-3">Nombre</th><th class="px-3 py-3">Tipo</th><th class="px-3 py-3">Detalles</th><th class="px-3 py-3">Estado</th><th class="px-3 py-3 text-right">Acciones</th></tr></thead><tbody class="divide-y divide-slate-100">@forelse($accounts as $account)<tr><td class="px-3 py-3 font-medium">{{ $account->name }}</td><td class="px-3 py-3">{{ ['qr' => 'QR', 'transfer' => 'Transferencia', 'bank' => 'Banco'][$account->type] }}</td><td class="px-3 py-3">{{ $account->details ?: '—' }}</td><td class="px-3 py-3">{{ $account->is_active ? 'Activa' : 'Inactiva' }}</td><td class="px-3 py-3 text-right">@can('update', $account)<button wire:click="edit({{ $account->id }})" class="text-indigo-600">Editar</button><button wire:click="toggle({{ $account->id }})" class="ml-3 text-slate-600">{{ $account->is_active ? 'Desactivar' : 'Activar' }}</button>@endcan @can('delete', $account)<button wire:click="delete({{ $account->id }})" class="ml-3 text-rose-600">Eliminar</button>@endcan</td></tr>@empty<tr><td colspan="5" class="px-3 py-10 text-center text-slate-500">No hay cuentas configuradas.</td></tr>@endforelse</tbody></table></div>
    <x-modal name="payment-account" focusable><form wire:submit="save" class="p-6"><h2 class="text-lg font-semibold">{{ $editingId ? 'Editar cuenta' : 'Nueva cuenta' }}</h2><div class="mt-5 space-y-4"><div><x-input-label for="account-name" value="Nombre" /><x-text-input id="account-name" wire:model="name" class="mt-1 block w-full" /><x-input-error :messages="$errors->get('name')" /></div><div><x-input-label for="account-type" value="Tipo" /><select id="account-type" wire:model="type" class="mt-1 block w-full rounded-md border-slate-300"><option value="qr">QR</option><option value="transfer">Transferencia</option><option value="bank">Banco</option></select><x-input-error :messages="$errors->get('type')" /></div><div><x-input-label for="account-details" value="Número o detalles" /><x-text-input id="account-details" wire:model="details" class="mt-1 block w-full" /><x-input-error :messages="$errors->get('details')" /></div></div><div class="mt-6 flex justify-end gap-3"><x-secondary-button type="button" x-on:click="$dispatch('close')">Cancelar</x-secondary-button><x-primary-button>Guardar</x-primary-button></div></form></x-modal>
</div>

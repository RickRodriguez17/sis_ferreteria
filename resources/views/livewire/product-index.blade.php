<div>
    <div class="mb-6 flex items-center justify-between"><div><h1 class="text-2xl font-bold text-slate-900">Productos</h1><p class="text-sm text-slate-500">Catálogo, precios, presentaciones e inventario.</p></div>@can('create', \App\Models\Product::class)<a href="{{ route('products.create') }}" class="rounded-lg bg-indigo-600 px-4 py-2 text-sm font-semibold text-white">Nuevo producto</a>@endcan</div>
    @if(session('success'))<div class="mb-4 rounded-lg bg-emerald-50 p-3 text-sm text-emerald-700">{{ session('success') }}</div>@endif
    <div class="rounded-xl bg-white p-5 shadow-sm">
        <x-table-toolbar wire:model.live.debounce.300ms="search"><select wire:model.live="categoryId" class="rounded-lg border-slate-300 text-sm"><option value="">Categorías</option>@foreach($categories as $category)<option value="{{ $category->id }}">{{ $category->name }}</option>@endforeach</select><select wire:model.live="brandId" class="rounded-lg border-slate-300 text-sm"><option value="">Marcas</option>@foreach($brands as $brand)<option value="{{ $brand->id }}">{{ $brand->name }}</option>@endforeach</select><select wire:model.live="locationId" class="rounded-lg border-slate-300 text-sm"><option value="">Ubicación</option>@foreach($locations as $location)<option value="{{ $location->id }}">{{ $location->name }}</option>@endforeach</select><label class="flex items-center gap-2 text-sm"><input type="checkbox" wire:model.live="belowMinimum" class="rounded border-slate-300 text-indigo-600"> Bajo mínimo</label></x-table-toolbar>
        <div class="mt-5 overflow-x-auto"><table class="w-full text-left text-sm"><thead class="border-b text-xs uppercase text-slate-500"><tr><th class="px-3 py-3">Producto</th><th class="px-3 py-3">Categoría</th><th class="px-3 py-3">Marca</th><th class="px-3 py-3">Stock</th><th class="px-3 py-3">Estado</th><th class="px-3 py-3 text-right">Acciones</th></tr></thead><tbody class="divide-y divide-slate-100">@forelse($products as $product) @php($stock = $product->inventories->sum('quantity'))<tr wire:key="product-{{ $product->id }}"><td class="px-3 py-3"><div class="font-semibold">{{ $product->name }}</div><div class="text-xs text-slate-500">{{ $product->code }} · {{ $product->barcode ?: 'sin código de barras' }}</div></td><td class="px-3 py-3">{{ $product->category?->name }}</td><td class="px-3 py-3">{{ $product->brand?->name }}</td><td class="px-3 py-3"><span class="{{ $product->min_stock !== null && (float) $stock < (float) $product->min_stock ? 'font-bold text-rose-600' : '' }}">{{ $stock }} {{ $product->unit?->abbreviation }}</span></td><td class="px-3 py-3"><span class="rounded-full px-2 py-1 text-xs {{ $product->is_active ? 'bg-emerald-100 text-emerald-700' : 'bg-slate-100 text-slate-500' }}">{{ $product->is_active ? 'Activo' : 'Inactivo' }}</span></td><td class="px-3 py-3 text-right">@can('update', $product)<a href="{{ route('products.edit', $product) }}" class="text-indigo-600">Editar</a>@endcan @can('delete', $product)<button wire:click="delete({{ $product->id }})" wire:confirm="¿Eliminar este producto?" class="ml-3 text-rose-600">Eliminar</button>@endcan</td></tr>@empty<tr><td colspan="6" class="px-3 py-12 text-center text-slate-500">No se encontraron productos.</td></tr>@endforelse</tbody></table></div><div class="mt-4">{{ $products->links() }}</div>
    </div>
</div>

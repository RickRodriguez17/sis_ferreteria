<div><div class="mb-6 flex items-center justify-between"><div><h1 class="text-2xl font-bold text-slate-900">Compras</h1><p class="text-sm text-slate-500">Órdenes de compra y recepciones.</p></div>@can('create', \App\Models\Purchase::class)<a href="{{ route('purchases.create') }}" class="rounded-lg bg-indigo-600 px-4 py-2 text-sm font-semibold text-white">Nueva compra</a>@endcan</div>@if(session('success'))<div class="mb-4 rounded-lg bg-emerald-50 p-3 text-sm text-emerald-700">{{ session('success') }}</div>@endif<div class="rounded-xl bg-white p-5 shadow-sm"><x-table-toolbar wire:model.live.debounce.300ms="search"><select wire:model.live="status" class="rounded-lg border-slate-300 text-sm"><option value="">Estados</option>@foreach($statuses as $item)<option value="{{ $item->value }}">{{ $item->name }}</option>@endforeach</select><select wire:model.live="supplierId" class="rounded-lg border-slate-300 text-sm"><option value="">Proveedores</option>@foreach($suppliers as $supplier)<option value="{{ $supplier->id }}">{{ $supplier->name }}</option>@endforeach</select><input type="date" wire:model.live="from" class="rounded-lg border-slate-300 text-sm"><input type="date" wire:model.live="to" class="rounded-lg border-slate-300 text-sm"></x-table-toolbar><div class="mt-5 overflow-x-auto"><table class="w-full text-left text-sm"><thead class="border-b text-xs uppercase text-slate-500"><tr><th class="px-3 py-3">Compra</th><th class="px-3 py-3">Proveedor</th><th class="px-3 py-3">Total</th><th class="px-3 py-3">Líneas</th><th class="px-3 py-3">Recepciones</th><th class="px-3 py-3">Estado</th><th class="px-3 py-3 text-right">Acciones</th></tr></thead><tbody class="divide-y divide-slate-100">@forelse($purchases as $purchase)<tr><td class="px-3 py-3"><div class="font-medium">{{ $purchase->code }}</div><div class="text-xs text-slate-500">{{ $purchase->created_at?->format('d/m/Y') }}</div></td><td class="px-3 py-3">{{ $purchase->supplier->name }}</td><td class="px-3 py-3">{{ $purchase->total }}</td><td class="px-3 py-3">{{ $purchase->items_count }}</td><td class="px-3 py-3">{{ $purchase->receptions_count }}</td><td class="px-3 py-3"><span class="rounded-full bg-indigo-100 px-2 py-1 text-xs text-indigo-700">{{ $purchase->status->name }}</span></td><td class="px-3 py-3 text-right"><a href="{{ route('purchases.show', $purchase) }}" class="text-indigo-600">Ver</a>@if(in_array($purchase->status->value, ['pending', 'partial'], true))<a href="{{ route('purchases.edit', $purchase) }}" class="ml-3 text-slate-600">Editar</a>@endif</td></tr>@empty<tr><td colspan="7" class="px-3 py-12 text-center text-slate-500">No hay compras.</td></tr>@endforelse</tbody></table></div><div class="mt-4">{{ $purchases->links() }}</div></div></div>

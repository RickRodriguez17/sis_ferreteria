<div>
    <div class="mb-6"><a href="{{ route('quotations.index') }}" class="text-sm text-indigo-600">← Cotizaciones</a><h1 class="mt-2 text-2xl font-bold text-slate-900">{{ $quotation ? 'Editar cotización' : 'Nueva cotización' }}</h1></div>
    <div class="grid gap-6 lg:grid-cols-3"><section class="space-y-6 lg:col-span-2"><div class="rounded-xl bg-white p-6 shadow-sm"><div class="flex items-center justify-between"><h2 class="font-semibold">Datos de la cotización</h2><label class="flex items-center gap-2 text-sm"><input type="checkbox" wire:model.live="withInvoice" class="rounded border-slate-300 text-indigo-600"> Con factura</label></div><div class="mt-4 grid gap-4 sm:grid-cols-2"><div><x-input-label value="Cliente"/><select wire:model="customerId" class="mt-1 w-full rounded-lg border-slate-300"><option value="">Cliente ocasional</option>@foreach($customers as $customer)<option value="{{ $customer->id }}">{{ $customer->name }}</option>@endforeach</select></div><div><x-input-label value="Vigencia"/><x-text-input type="date" wire:model="validUntil" class="mt-1 w-full"/></div></div><div class="mt-4"><x-text-input wire:model.live="customerSearch" class="w-full" placeholder="Buscar cliente..."/></div></div><div class="rounded-xl bg-white p-6 shadow-sm"><h2 class="font-semibold">Productos</h2><div class="relative mt-4"><x-text-input wire:model.live.debounce.300ms="productSearch" class="w-full" placeholder="Buscar por nombre, código, barra, marca o atributo..."/>@if($productSearch !== '')<div class="absolute z-10 mt-1 w-full rounded-lg border bg-white shadow">@foreach($products as $product)<button type="button" wire:click="selectProduct({{ $product->id }})" class="block w-full px-4 py-2 text-left text-sm hover:bg-slate-50">{{ $product->name }} ({{ $product->code }})</button>@endforeach</div>@endif</div><div class="mt-5 space-y-3">@forelse($items as $index => $item)<div wire:key="quotation-item-{{ $index }}" class="grid gap-2 rounded-lg bg-slate-50 p-3 sm:grid-cols-12"><div class="font-medium text-sm sm:col-span-5">{{ \App\Models\Product::find($item['product_id'])?->name }}<div class="text-xs text-slate-500">Presentación seleccionada</div></div><input wire:model.live="items.{{ $index }}.quantity" type="number" min="0.0001" step="0.0001" class="rounded border-slate-300 text-sm sm:col-span-2" placeholder="Cantidad"><input wire:model.live="items.{{ $index }}.unit_price" type="number" min="0" step="0.01" class="rounded border-slate-300 text-sm sm:col-span-2" placeholder="Precio"><div class="py-2 text-sm font-semibold sm:col-span-2">{{ number_format((float) ($item['subtotal'] ?? 0), 2) }}</div><button type="button" wire:click="removeItem({{ $index }})" class="text-sm text-rose-600">Quitar</button></div>@empty<div class="py-10 text-center text-sm text-slate-500">Busca un producto para agregarlo.</div>@endforelse</div></div></section><aside class="rounded-xl bg-white p-6 shadow-sm"><h2 class="font-semibold">Resumen</h2><div class="mt-6 flex justify-between border-t pt-4 text-lg font-bold"><span>Total</span><span>{{ number_format((float) collect($items)->sum('subtotal'), 2) }}</span></div><button type="button" wire:click="save" class="mt-6 w-full rounded-lg bg-indigo-600 px-5 py-3 text-sm font-semibold text-white">Guardar cotización</button></aside></div>
    @can('create', \App\Models\Customer::class)<livewire:customer-form :in-modal="true" />@endcan
</div>

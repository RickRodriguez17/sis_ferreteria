<div>
    <div class="mb-6 flex items-start justify-between"><div><a href="{{ route('quotations.index') }}" class="text-sm text-indigo-600">← Cotizaciones</a><h1 class="mt-2 text-2xl font-bold text-slate-900">Cotización {{ $quotation->code }}</h1><p class="text-sm text-slate-500">{{ $quotation->created_at?->format('d/m/Y H:i') }}</p></div><div class="flex gap-2"><a href="{{ route('quotations.pdf', $quotation) }}" class="rounded-lg border border-slate-300 bg-white px-4 py-2 text-sm">Descargar PDF</a>@can('update', $quotation) @if($quotation->status->value === 'open')<a href="{{ route('quotations.edit', $quotation) }}" class="rounded-lg border border-slate-300 bg-white px-4 py-2 text-sm">Editar</a>@endif @endcan @can('create', \App\Models\Sale::class) @if($quotation->status->value === 'open')<button wire:click="convert" class="rounded-lg bg-indigo-600 px-4 py-2 text-sm font-semibold text-white">Convertir en venta</button>@endif @endcan</div></div>
    @if($errors->any())<div class="mb-4 rounded-lg bg-rose-50 p-3 text-sm text-rose-700">@foreach($errors->all() as $error)<div>{{ $error }}</div>@endforeach</div>@endif
    <div class="rounded-xl bg-white p-6 shadow-sm"><div class="grid gap-4 sm:grid-cols-3"><div><p class="text-xs uppercase text-slate-500">Cliente</p><p class="mt-1 font-semibold">{{ $quotation->customer?->name ?: 'Cliente ocasional' }}</p></div><div><p class="text-xs uppercase text-slate-500">Vigencia</p><p class="mt-1 font-semibold">{{ $quotation->valid_until?->format('d/m/Y') ?: 'Sin fecha' }}</p></div><div><p class="text-xs uppercase text-slate-500">Estado</p><p class="mt-1 font-semibold">{{ ['open' => 'Abierta', 'converted' => 'Convertida', 'expired' => 'Vencida', 'cancelled' => 'Cancelada'][$quotation->status->value] }}</p></div></div><div class="mt-6 overflow-x-auto"><table class="w-full text-left text-sm"><thead class="border-b text-xs uppercase text-slate-500"><tr><th class="px-2 py-2">Producto</th><th class="px-2 py-2">Presentación</th><th class="px-2 py-2">Cantidad</th><th class="px-2 py-2">Precio</th><th class="px-2 py-2 text-right">Subtotal</th></tr></thead><tbody class="divide-y divide-slate-100">@foreach($quotation->items as $item)<tr><td class="px-2 py-2">{{ $item->product->name }}</td><td class="px-2 py-2">{{ $item->presentation?->name ?: 'Unidad' }}</td><td class="px-2 py-2">{{ $item->quantity }}</td><td class="px-2 py-2">{{ number_format((float) $item->unit_price, 2) }}</td><td class="px-2 py-2 text-right">{{ number_format((float) $item->subtotal, 2) }}</td></tr>@endforeach</tbody></table></div><div class="mt-6 text-right text-lg font-bold">Total: {{ number_format((float) $quotation->total, 2) }}</div></div>
</div>

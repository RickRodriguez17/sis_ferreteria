<div>
    <x-pagetitle title="Ventas" icon="bi-cart-check" section="Ventas" subtitle="Consulta ventas, pagos y movimientos de inventario.">
        <x-slot:actions>
            @can('create', \App\Models\Sale::class)<a href="{{ route('sales.create') }}" class="rounded-lg bg-indigo-600 px-4 py-2 text-sm font-semibold text-white">Nueva venta</a>@endcan
        </x-slot:actions>
    </x-pagetitle>
    <div class="rounded-xl bg-white p-5 shadow-sm"><x-table-toolbar wire:model.live.debounce.300ms="search"><select wire:model.live="status" class="rounded-lg border-slate-300 text-sm"><option value="">Estados</option>@foreach($statuses as $item)<option value="{{ $item->value }}">{{ $item->value === 'completed' ? 'Completada' : 'Cancelada' }}</option>@endforeach</select><select wire:model.live="customerId" class="rounded-lg border-slate-300 text-sm"><option value="">Clientes</option>@foreach($customers as $customer)<option value="{{ $customer->id }}">{{ $customer->name }}</option>@endforeach</select><select wire:model.live="withInvoice" class="rounded-lg border-slate-300 text-sm"><option value="">Factura</option><option value="yes">Con factura</option><option value="no">Sin factura</option></select><input type="date" wire:model.live="from" class="rounded-lg border-slate-300 text-sm"><input type="date" wire:model.live="to" class="rounded-lg border-slate-300 text-sm"></x-table-toolbar><div class="mt-5 overflow-x-auto"><table class="w-full text-left text-sm"><thead class="border-b text-xs uppercase text-slate-500"><tr><th class="px-3 py-3">Venta</th><th class="px-3 py-3">Cliente</th><th class="px-3 py-3">Pago</th><th class="px-3 py-3">Total</th><th class="px-3 py-3">Estado</th><th class="px-3 py-3 text-right">Acciones</th></tr></thead><tbody class="divide-y divide-slate-100">@forelse($sales as $sale)<tr><td class="px-3 py-3"><div class="font-medium">{{ $sale->code }}</div><div class="text-xs text-slate-500">{{ $sale->created_at?->format('d/m/Y') }}</div></td><td class="px-3 py-3">{{ $sale->customer?->name ?: 'Cliente ocasional' }}</td><td class="px-3 py-3">{{ ['cash' => 'Contado', 'credit' => 'Crédito', 'mixed' => 'Mixto'][$sale->payment_type->value] ?? $sale->payment_type->value }}</td><td class="px-3 py-3">{{ number_format((float) $sale->total, 2) }}</td><td class="px-3 py-3">{{ $sale->status->value === 'completed' ? 'Completada' : 'Cancelada' }}</td><td class="px-3 py-3 text-right"><a href="{{ route('sales.show', $sale) }}" class="text-indigo-600">Ver</a>@can('delete', $sale) @if($sale->status->value === 'completed')<button wire:click="cancel({{ $sale->id }})" wire:confirm="¿Cancelar esta venta?" class="ml-3 text-rose-600">Cancelar</button>@endif @endcan</td></tr>@empty<tr><td colspan="6" class="px-3 py-12 text-center text-slate-500">No hay ventas.</td></tr>@endforelse</tbody></table></div><div class="mt-4">{{ $sales->links() }}</div></div>
</div>

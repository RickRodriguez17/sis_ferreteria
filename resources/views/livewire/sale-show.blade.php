<div>
    <div class="mb-6 flex items-start justify-between"><div><a href="{{ route('sales.index') }}" class="text-sm text-indigo-600">← Ventas</a><h1 class="mt-2 text-2xl font-bold text-slate-900">Venta {{ $sale->code }}</h1><p class="text-sm text-slate-500">{{ $sale->created_at?->format('d/m/Y H:i') }}</p></div><div class="flex gap-2">@can('prices.update') @if($sale->items->contains('price_pending', true))<button wire:click="openPendingPrices" class="rounded-lg bg-indigo-600 px-4 py-2 text-sm font-semibold text-white">Definir precios</button>@endif @endcan<a href="{{ route('sales.pdf', $sale) }}" class="rounded-lg border border-slate-300 bg-white px-4 py-2 text-sm">Descargar PDF</a></div></div>
    @if(session('success'))<div class="mb-4 rounded-lg bg-emerald-50 p-3 text-sm text-emerald-700">{{ session('success') }}</div>@endif
    <div class="grid gap-4 md:grid-cols-4"><div class="rounded-xl bg-white p-5 shadow-sm"><p class="text-xs uppercase text-slate-500">Cliente</p><p class="mt-2 font-semibold">{{ $sale->customer?->name ?: 'Cliente ocasional' }}</p></div><div class="rounded-xl bg-white p-5 shadow-sm"><p class="text-xs uppercase text-slate-500">Pago</p><p class="mt-2 font-semibold">{{ $sale->payment_type->label() }}</p></div><div class="rounded-xl bg-white p-5 shadow-sm"><p class="text-xs uppercase text-slate-500">Factura</p><p class="mt-2 font-semibold">{{ $sale->with_invoice ? 'Con factura' : 'Sin factura' }}</p></div><div class="rounded-xl bg-white p-5 shadow-sm"><p class="text-xs uppercase text-slate-500">Estado</p><p class="mt-2 font-semibold">{{ $sale->status->label() }}</p></div></div>
    <div class="mt-6 rounded-xl bg-white p-6 shadow-sm"><h2 class="font-semibold">Productos</h2><div class="mt-4 overflow-x-auto"><table class="w-full text-left text-sm"><thead class="border-b text-xs uppercase text-slate-500"><tr><th class="px-2 py-2">Producto</th><th class="px-2 py-2">Presentación</th><th class="px-2 py-2">Cantidad</th><th class="px-2 py-2">Precio</th><th class="px-2 py-2 text-right">Subtotal</th></tr></thead><tbody class="divide-y divide-slate-100">@foreach($sale->items as $item)<tr><td class="px-2 py-2">{{ $item->product->name }}</td><td class="px-2 py-2">{{ $item->presentation?->name ?: 'Unidad' }}</td><td class="px-2 py-2">{{ qty($item->quantity) }}</td><td class="px-2 py-2">@if($item->price_pending)<span class="rounded-full bg-amber-100 px-2 py-1 text-xs font-medium text-amber-800">Precio pendiente</span>@else{{ money((float) $item->unit_price) }}@endif</td><td class="px-2 py-2 text-right">{{ money((float) $item->subtotal) }}</td></tr>@endforeach</tbody></table></div><div class="mt-6 ml-auto max-w-xs space-y-2 text-sm"><div class="flex justify-between"><span>Subtotal</span><span>{{ money((float) $sale->subtotal) }}</span></div><div class="flex justify-between"><span>Descuento</span><span>{{ money((float) $sale->discount) }}</span></div><div class="flex justify-between text-lg font-bold"><span>Total</span><span>{{ money((float) $sale->total) }}</span></div></div></div>
    @if($sale->credit)<div class="mt-6 rounded-xl bg-white p-6 shadow-sm"><h2 class="font-semibold">Crédito</h2><p class="mt-2 text-sm">Saldo pendiente: <strong>{{ money((float) $sale->credit->balance) }}</strong></p></div>@endif
    @if($showPendingPrices)<div class="fixed inset-0 z-50 flex items-center justify-center bg-slate-950/50 p-4"><div class="w-full max-w-lg rounded-xl bg-white p-6 shadow-xl"><h2 class="text-lg font-semibold">Definir precios pendientes</h2><div class="mt-4 space-y-3">@foreach($sale->items->where('price_pending', true) as $item)<div class="flex items-center justify-between gap-3"><span class="text-sm">{{ $item->product->name }} · {{ qty($item->quantity) }}</span><x-text-input type="number" min="0" step="0.01" wire:model="pendingPrices.{{ $item->id }}" class="w-36" placeholder="Precio"></x-text-input></div>@endforeach</div>@error('pendingPrices')<p class="mt-3 text-sm text-rose-600">{{ $message }}</p>@enderror<div class="mt-6 flex justify-end gap-2"><button wire:click="$set('showPendingPrices', false)" class="rounded-lg border px-4 py-2 text-sm">Cancelar</button><button wire:click="definePendingPrices" class="rounded-lg bg-indigo-600 px-4 py-2 text-sm font-semibold text-white">Guardar precios</button></div></div></div>@endif
</div>

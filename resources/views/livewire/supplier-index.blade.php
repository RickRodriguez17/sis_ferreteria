<div>
    <div class="mb-6 flex items-center justify-between"><div><h1 class="text-2xl font-bold text-slate-900">Proveedores</h1><p class="text-sm text-slate-500">Administra proveedores y su historial de compras.</p></div>@can('create', \App\Models\Supplier::class)<button wire:click="create" class="rounded-lg bg-indigo-600 px-4 py-2 text-sm font-semibold text-white">Agregar proveedor</button>@endcan</div>
    @if(session('success'))<div class="mb-4 rounded-lg bg-emerald-50 p-3 text-sm text-emerald-700">{{ session('success') }}</div>@endif
    <div class="rounded-xl bg-white p-5 shadow-sm"><x-table-toolbar wire:model.live.debounce.300ms="search"><select wire:model.live="status" class="rounded-lg border-slate-300 text-sm"><option value="">Todos</option><option value="active">Activos</option><option value="inactive">Inactivos</option></select></x-table-toolbar><div class="mt-5 overflow-x-auto"><table class="w-full text-left text-sm"><thead class="border-b text-xs uppercase text-slate-500"><tr><th class="px-3 py-3">Proveedor</th><th class="px-3 py-3">Contacto</th><th class="px-3 py-3">Compras</th><th class="px-3 py-3">Estado</th><th class="px-3 py-3 text-right">Acciones</th></tr></thead><tbody class="divide-y divide-slate-100">@forelse($suppliers as $supplier)<tr><td class="px-3 py-3"><div class="font-medium">{{ $supplier->name }}</div><div class="text-xs text-slate-500">{{ $supplier->document_number ?: 'Sin documento' }}</div></td><td class="px-3 py-3">{{ $supplier->phone ?: $supplier->email ?: '—' }}</td><td class="px-3 py-3">{{ $supplier->purchases_count }}</td><td class="px-3 py-3"><span class="rounded-full px-2 py-1 text-xs {{ $supplier->is_active ? 'bg-emerald-100 text-emerald-700' : 'bg-slate-100 text-slate-500' }}">{{ $supplier->is_active ? 'Activo' : 'Inactivo' }}</span></td><td class="px-3 py-3 text-right"><a href="{{ route('purchases.index', ['supplier_id' => $supplier->id]) }}" class="text-slate-600">Historial</a><button wire:click="edit({{ $supplier->id }})" class="ml-3 text-indigo-600">Editar</button><button wire:click="toggle({{ $supplier->id }})" class="ml-3 text-amber-600">{{ $supplier->is_active ? 'Desactivar' : 'Activar' }}</button><button wire:click="delete({{ $supplier->id }})" wire:confirm="¿Eliminar este proveedor?" class="ml-3 text-rose-600">Eliminar</button></td></tr>@empty<tr><td colspan="5" class="px-3 py-12 text-center text-slate-500">No hay proveedores.</td></tr>@endforelse</tbody></table></div><div class="mt-4">{{ $suppliers->links() }}</div></div>
    <x-modal name="supplier-record"><div class="p-6"><h2 class="text-lg font-semibold">{{ $editingId ? 'Editar' : 'Agregar' }} proveedor</h2><div class="mt-4 grid gap-4 sm:grid-cols-2"><div class="sm:col-span-2"><x-input-label value="Nombre"/><x-text-input wire:model="name" class="mt-1 w-full"/><x-input-error :messages="$errors->get('name')"/></div><div><x-input-label value="Tipo de documento"/><x-text-input wire:model="documentType" class="mt-1 w-full"/></div><div><x-input-label value="Número de documento"/><x-text-input wire:model="documentNumber" class="mt-1 w-full"/></div><div><x-input-label value="Teléfono"/><x-text-input wire:model="phone" class="mt-1 w-full"/></div><div><x-input-label value="Email"/><x-text-input type="email" wire:model="email" class="mt-1 w-full"/></div><div class="sm:col-span-2"><x-input-label value="Dirección"/><x-text-input wire:model="address" class="mt-1 w-full"/></div><label class="flex items-center gap-2 text-sm"><input type="checkbox" wire:model="isActive" class="rounded border-slate-300 text-indigo-600"> Activo</label></div><div class="mt-6 flex justify-end gap-3"><button wire:click="closeModal" class="rounded-lg border px-4 py-2">Cancelar</button><button wire:click="save" class="rounded-lg bg-indigo-600 px-4 py-2 text-white">Guardar</button></div></div></x-modal>
</div>
